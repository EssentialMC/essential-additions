package com.spikespaz.eamod.main;

public class EventHandler {
//    @SubscribeEvent
//    public void BreakEvent(BlockEvent.BreakEvent event) {
//        // Get tool used to break the block.
//        ItemStack toolUsed = event.getPlayer().getHeldItemMainhand();
//        Item toolItem = toolUsed.getItem();
//        // Drop itself when a mob spawner is broken.
//        if (event.getState().getBlock() instanceof BlockMobSpawner && !event.getWorld().isRemote && toolItem.ToolMaterial.getHarvestLevel() >= 2) {
//            ItemStack drop = new ItemStack(Item.getItemFromBlock(event.getState().getBlock()));
//            spawnAsEntity(event.getWorld(), event.getPos(), drop);
//        }
//    }
}
